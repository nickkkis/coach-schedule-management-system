version: '3.9'

services:
  django:
    image: django-docker:0.0.1
    build: .
    ports:
      - "8000:8000"

  db:
    image: postgres:latest
    environment:
      POSTGRES_DB: csms_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin